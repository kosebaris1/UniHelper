@* @model List<HayvanDto.PetDtos.GetWaitingPetDto>

<div class="col-lg-6 mb-4">
    <div class="card shadow">
        <div class="card-header py-3 bg-primary text-white">
            <h6 class="m-0 font-weight-bold">Onay Bekleyen İlanlar</h6>
        </div>
        <div class="card-body">
            <div class="table-responsive" style="max-height: 360px; overflow-y: auto;">
                <table class="table table-bordered mb-0">
                    <thead class="table-dark" style="position: sticky; top: 0; z-index: 1;">
                        <tr>
                            <th>ID</th>
                            <th>Hayvan</th>
                            <th>Tür</th>
                            <th>Kullanıcı</th>
                            <th>Tarih</th>
                            <th>İşlem</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.PetId</td>
                                <td>@item.Name</td>
                                <td>@item.PetTypeName</td>
                                <td>@item.UserName</td>
                                <td>@item.CreatedDate.ToString("dd.MM.yyyy HH:mm")</td>
                                <td>
                                    <div class="btn-group btn-group-sm" role="group">
                                        <!-- Onayla -->
                                        <form asp-area="Admin" asp-controller="AdminMain" asp-action="Accept" method="post" style="display:inline;">
                                            <input type="hidden" name="id" value="@item.PetId" />
                                            <button type="submit" class="btn btn-success">Onayla</button>
                                        </form>

                                        <!-- Reddet -->
                                        <form asp-area="Admin" asp-controller="AdminMain" asp-action="Reject" method="post" style="display:inline;">
                                            <input type="hidden" name="id" value="@item.PetId" />
                                            <button type="submit" class="btn btn-danger">Reddet</button>
                                        </form>

                                        <!-- Görüntüle -->
                                        <a asp-area="" asp-controller="Pet" asp-action="Detail" asp-route-id="@item.PetId" class="btn btn-primary btn-sm">Görüntüle</a>
                                        <!-- Yol: elle verdim sen burada değiştirebilirsin -->
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<style>
    th {
        white-space: nowrap;
        text-align: center;
        vertical-align: middle;
    }

    td {
        vertical-align: middle;
        text-align: center;
    }

    form {
        margin: 0 2px;
    }
</style>
 *@